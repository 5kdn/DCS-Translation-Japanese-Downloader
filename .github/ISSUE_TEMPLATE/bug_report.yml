name: 🐛 バグ報告
description: 不具合や予期しない挙動を報告するテンプレート
title: "[Bug]: "
labels: [bug]
body:
  - type: checkboxes
    id: checklist
    attributes:
      label: 事前確認
      description: Issue 作成前に以下を確認してください
      options:
        - label: 最新の `master`（またはデプロイ済みバージョン）で再現することを確認しました
          required: true
        - label: 既存 Issue / Discussion / Discord で同様の報告がないか確認しました
          required: true

  - type: input
    id: summary
    attributes:
      label: 概要
      description: 発生した不具合を 1 行でまとめてください
      placeholder: "例: 設定画面で保存が反映されない"
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: 再現手順
      description: バグを再現する手順をステップごとに記載してください
      placeholder: |
        例:
        1. 設定画面を開く
        2. 入力内容を変更
        3. 保存ボタンを押す
        4. 反映状況を確認する
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: 期待される動作
      description: 本来どのように動作すべきかを記載してください
      placeholder: "例: 保存後、設定内容が即座に反映される"
    validations:
      required: true

  - type: textarea
    id: actual
    attributes:
      label: 実際の動作
      description: 実際に確認された動作を具体的に記載してください
      placeholder: "例: 保存ボタンを押しても反応がなく、エラーが表示される"
    validations:
      required: true

  - type: textarea
    id: environment
    attributes:
      label: 実行環境
      description: 発生環境をできるだけ詳しく記載してください
      placeholder: |
        例:
        - OS: Windows 11
        - ブラウザ: Chrome 114
        - Node.js: v20.11.1
        - pnpm: 9.12.0
        - wrangler: 4.0.0
        - Cloudflare Worker bindings: NODE_ENV=development, AllowOrigins=http://localhost:8787
    validations:
      required: true

  - type: textarea
    id: logs
    attributes:
      label: ログ / スタックトレース
      description: wrangler のターミナルログや Cloudflare ダッシュボードのエラーを貼り付けてください（機微情報は伏字にする）
      placeholder: "例: [2025-11-05T12:34:56Z] ERROR Octokit request failed..."
    validations:
      required: false

  - type: textarea
    id: additional
    attributes:
      label: 補足情報
      description: エラーログやスクリーンショットがあれば貼り付けてください（機微情報は伏せてください）
      placeholder: "例: スタックトレース、コンソールログ、画像URL など"
      required: false

  - type: textarea
    id: notes
    attributes:
      label: 備考
      description: 回避策、影響範囲、関連Issue/PRなどがあれば記載してください
      placeholder: "例: 一時的にA設定を無効化すると回避可能 / 関連 #123"
    validations:
      required: false
